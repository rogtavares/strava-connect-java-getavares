# 🏗️ Arquitetura - Strava Connect Completo

## Visão Geral da Arquitetura

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           CLIENTE (User)                                │
│                                                                         │
│                         Portfolio Site                                 │
│               (Next.js + TypeScript + Tailwind)                        │
└──────────────────┬──────────────────────────────────────────────────────┘
                   │
                   │ HTTPS
                   │
┌──────────────────▼──────────────────────────────────────────────────────┐
│                        CLOUDFRONT (CDN)                                │
│  - Cache estático (HTML, CSS, JS)                                      │
│  - Cache dinâmico (API responses)                                      │
│  - CORS headers                                                         │
│  - WAF protection                                                       │
└──────────────────┬──────────────────────────────────────────────────────┘
                   │
                   │ API Calls
                   │
┌──────────────────▼──────────────────────────────────────────────────────┐
│                     API GATEWAY (REST API)                             │
│                                                                         │
│  Routes:                                                                │
│  - POST /auth/callback              → Lambda: authCallback            │
│  - GET  /athlete/{user_id}          → Lambda: getAthlete              │
│  - GET  /activities/{user_id}       → Lambda: getActivities           │
│  - GET  /stats/{user_id}            → Lambda: getStats                │
│  - GET  /insights/{user_id}         → Lambda: getInsights             │
└──────────────────┬──────────────────────────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        │                     │
┌───────▼────────┐    ┌──────▼────────────┐
│  AWS LAMBDA    │    │   DYNAMODB       │
│                │    │   (NoSQL DB)     │
│ ┌────────────┐ │    │                  │
│ │ Auth       │ │    │ ┌──────────────┐ │
│ │ Handler    │ │    │ │ Users Table  │ │
│ └────────────┘ │    │ └──────────────┘ │
│                │    │                  │
│ ┌────────────┐ │    │ ┌──────────────┐ │
│ │ Athlete    │ │    │ │ Activities   │ │
│ │ Handler    │ │    │ │ Table        │ │
│ └────────────┘ │    │ └──────────────┘ │
│                │    │                  │
│ ┌────────────┐ │    │ ┌──────────────┐ │
│ │ Activities │ │    │ │ Cache Table  │ │
│ │ Handler    │ │    │ │ (TTL)        │ │
│ └────────────┘ │    │ └──────────────┘ │
│                │    │                  │
│ ┌────────────┐ │    │                  │
│ │ Stats      │ │    │                  │
│ │ Handler    │ │    │                  │
│ └────────────┘ │    │                  │
│                │    │                  │
│ ┌────────────┐ │    │                  │
│ │ Insights   │ │    │                  │
│ │ Handler    │ │    │                  │
│ └────────────┘ │    │                  │
│                │    │                  │
│ Libraries:     │    │                  │
│ - boto3        │    │                  │
│ - requests     │    │                  │
│ - numpy        │    │                  │
│ - pandas       │    │                  │
│ - scikit-learn │    │                  │
└────────────────┘    └──────────────────┘
        │                     │
        └─────────────────────┘
                 │
                 │ SDK Calls
                 │
        ┌────────▼─────────┐
        │   AWS SERVICES   │
        ├──────────────────┤
        │ - CloudWatch     │
        │ - X-Ray          │
        │ - Secrets Mgr.   │
        │ - Parameter Store│
        └──────────────────┘
                 │
                 │
        ┌────────▼──────────┐
        │  STRAVA API       │
        │                   │
        │ GET /athlete      │
        │ GET /activities   │
        │ POST /oauth/token │
        └───────────────────┘
```

---

## 📊 Flow Detalhado

### 1. OAuth Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                       USER                                      │
│              (Portfolio Site - Browser)                         │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                    1. Clica "Connect Strava"
                           │
                           ▼
                    ┌──────────────────┐
                    │  Strava OAuth    │
                    │  Authorization   │
                    │    Endpoint      │
                    └─────────┬────────┘
                              │
                    2. User autoriza
                              │
                              ▼
                    ┌──────────────────┐
                    │ Redireciona para │
                    │ /auth/callback   │
                    │ + code param     │
                    └─────────┬────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  POST            │
                    │  /auth/callback  │
                    │  {code: "..."}   │
                    └─────────┬────────┘
                              │
                         3. Lambda
                        Valida code
                              │
                              ▼
                    ┌──────────────────┐
                    │ POST             │
                    │ /oauth/token     │
                    │ (Strava API)     │
                    └─────────┬────────┘
                              │
                         4. Recebe
                      access_token +
                     refresh_token
                              │
                              ▼
                    ┌──────────────────┐
                    │ Armazena em      │
                    │ DynamoDB Users   │
                    │ Table            │
                    └─────────┬────────┘
                              │
                         5. Response com
                          access_token
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BROWSER                                   │
│            (Salva token em sessionStorage)                       │
└─────────────────────────────────────────────────────────────────┘
```

### 2. Get Activities Flow

```
┌──────────────┐
│  User Browser│
│ GET /        │
│activities/123│
└───────┬──────┘
        │
        ▼
   ┌─────────────────┐
   │  API Gateway    │
   │  + QueryParams  │
   └────────┬────────┘
            │
            ▼
        ┌──────────────────────┐
        │ Lambda Handler       │
        │ activities_handler   │
        └────────┬─────────────┘
                 │
          1. Valida user_id
                 │
                 ▼
          ┌──────────────┐
          │ Cache Check  │
          │ (DynamoDB)   │
          └────┬────┬────┘
               │    │
         HIT   │    │ MISS
               ▼    ▼
            Return  Continue
                    │
                    ▼
          ┌──────────────────┐
          │ Get Token do     │
          │ User (DynamoDB)  │
          └─────────┬────────┘
                    │
                    ▼
          ┌──────────────────┐
          │ GET /            │
          │ athlete/         │
          │ activities       │
          │ (Strava API)     │
          └─────────┬────────┘
                    │
          2. Verifica se token
             expirou (401)
                    │
          ┌─────────┴─────────┐
          │                   │
          No              Sim (expirou)
          │                   │
          │                   ▼
          │        ┌────────────────────┐
          │        │ POST /oauth/token  │
          │        │ with refresh_token │
          │        │ (Strava API)       │
          │        └────────┬───────────┘
          │                 │
          │                 ▼
          │        ┌────────────────────┐
          │        │ Salva novo token   │
          │        │ (DynamoDB)         │
          │        └────────┬───────────┘
          │                 │
          └─────────┬───────┘
                    │
                    ▼
          ┌──────────────────────┐
          │ Processa atividades  │
          │ - Filtra por tipo    │
          │ - Formata resposta   │
          │ - Calcula paginação  │
          └────────┬─────────────┘
                   │
                   ▼
          ┌──────────────────────┐
          │ Armazena em Cache    │
          │ (DynamoDB Cache)     │
          │ com TTL (1800s)      │
          └────────┬─────────────┘
                   │
                   ▼
          ┌──────────────────────┐
          │ Response JSON com    │
          │ atividades + meta    │
          │ paginação            │
          └────────┬─────────────┘
                   │
                   ▼
┌──────────────────────────────────────────┐
│          Browser                         │
│  Recebe JSON + renderiza dashboard      │
└──────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         REQUEST                                 │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                    ┌──────▼──────┐
                    │ AWS WAF     │
                    │ - IP Block  │
                    │ - Rate Limit│
                    │ - SQL Inject│
                    └──────┬──────┘
                           │
                    ┌──────▼──────────┐
                    │ CloudFront      │
                    │ - HTTPS only    │
                    │ - Signed URLs   │
                    │ - CORS headers  │
                    └──────┬──────────┘
                           │
                    ┌──────▼──────────┐
                    │ API Gateway     │
                    │ - Auth (Bearer) │
                    │ - Throttling    │
                    │ - Validation    │
                    └──────┬──────────┘
                           │
                    ┌──────▼──────────┐
                    │ Lambda VPC      │
                    │ - Private subnet│
                    │ - NAT Gateway   │
                    │ - SG rules      │
                    └──────┬──────────┘
                           │
                    ┌──────▼──────────┐
                    │ AWS Secrets Mgr │
                    │ Decrypt Creds   │
                    │ On-the-fly      │
                    └──────┬──────────┘
                           │
                    ┌──────▼──────────┐
                    │ DynamoDB        │
                    │ - Encryption    │
                    │ - IAM Policies  │
                    │ - Point-in-time │
                    │   recovery      │
                    └──────────────────┘
```

---

## 📈 Scaling & Performance

### Lambda Concurrency

```
Default: 1000 concurrent executions
Per-function limit: Configure via console

Autoscaling:
- 0 → 1000: Linear scaling (< 1 sec)
- Cold start: ~1-2s (Python)
- Warm start: ~100ms
```

### DynamoDB Capacity

```
Billing Mode: PAY_PER_REQUEST
- Automatic scaling
- Per-request pricing
- Better for variable workloads

Estimated capacity:
- 100 active users
- 1000 activities/day
- Cost: ~$20-50/month
```

### Cache Strategy

```
┌─────────────────────────────────┐
│  Cache Levels                   │
├─────────────────────────────────┤
│ L1: CloudFront (Edge Caching)   │ TTL: 1h
│ L2: DynamoDB (App Cache)        │ TTL: 30m
│ L3: Strava API (Source)         │ N/A
└─────────────────────────────────┘

Cache Invalidation:
- Manual: DELETE /cache/{key}
- TTL-based: Auto-delete após TTL
- Event-based: Update on new activity
```

---

## 🔄 CI/CD Pipeline

```
┌──────────────────────────────────────────────┐
│        Developer Push to GitHub              │
└──────────────────────┬───────────────────────┘
                       │
                       ▼
           ┌───────────────────────┐
           │ GitHub Actions        │
           │ 1. Lint + Format      │
           │ 2. Run Tests          │
           │ 3. Check CVE          │
           └───────┬───────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
    ✅ Pass              ❌ Fail
        │                     │
        ▼                     ▼
    ┌────────┐            Reject
    │ Deploy │            PR
    │  Dev   │
    └───┬────┘
        │
        ▼
    ┌────────────┐
    │ Serverless │
    │ Deploy Dev │
    └───┬────────┘
        │
        ▼
    ┌────────────┐
    │ Run E2E    │
    │ Tests      │
    └───┬────────┘
        │
        ▼
    ┌────────────┐
    │ Merge      │
    │ to Main    │
    └───┬────────┘
        │
        ▼
    ┌────────────┐
    │ Deploy     │
    │ Production │
    └────────────┘
```

---

## 📊 Database Schema

### Users Table (DynamoDB)

```
Partition Key: user_id (String)

Attributes:
├── athlete_name (String)
├── athlete_email (String)
├── athlete_profile (String)
├── tokens (Map)
│   ├── access_token
│   ├── refresh_token
│   ├── expires_at
│   └── token_type
├── created_at (String - ISO 8601)
└── updated_at (String - ISO 8601)
```

### Activities Table (DynamoDB)

```
Partition Key: user_id (String)
Sort Key: activity_id (String)

Attributes:
├── name (String)
├── distance (Number)
├── moving_time (Number)
├── elevation_gain (Number)
├── average_speed (Number)
├── start_date (String)
├── cached_at (String)
└── ... (outros campos de atividade)

GSI:
└── activity_id (PK) - para buscas por atividade
```

### Cache Table (DynamoDB)

```
Partition Key: cache_key (String - SHA256)
TTL Attribute: expires_at

Attributes:
├── data (String - JSON)
├── created_at (String)
└── expires_at (Number - Unix timestamp)

TTL:
- Athlete: 1 hora
- Activities: 30 minutos
- Stats: 2 horas
```

---

## 🚨 Error Handling

```
┌────────────────────────────────────┐
│         Lambda Handler             │
└──────────────────┬─────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
    ✅ Success            ❌ Error
        │                     │
        ▼                     ▼
    Response 2xx      Classify Erro
    {                 │
      success: true   ├─ 400: Bad Request
      data: {...}     ├─ 401: Unauthorized
    }                 ├─ 403: Forbidden
                      ├─ 404: Not Found
                      ├─ 429: Rate Limited
                      ├─ 500: Server Error
                      └─ 503: Service Unavailable
                         │
                         ▼
                      Response Error
                      {
                        success: false
                        error: {
                          message: "..."
                          code: "..."
                        }
                      }
```

---

## 📞 Contato & Suporte

- GitHub: https://github.com/rogtavares/strava-connect
- Issues: GitHub Issues
- Email: seu_email@example.com
